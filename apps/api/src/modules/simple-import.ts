import type { createDatabaseConnection } from "@finances/database"; import type { FastifyInstance } from "fastify"; import { createTransactionImportService } from "../application/transaction-import-service.js";
export function registerSimpleImportRoutes(app: FastifyInstance, connection: ReturnType<typeof createDatabaseConnection>) { const service = createTransactionImportService(connection); app.post("/simple-import/preview", async (request) => service.preview((request.body as { transactions?: unknown[] }).transactions ?? [])); app.post("/simple-import/confirm", async (request, reply) => reply.code(201).send(service.confirm((request.body as { transactions?: unknown[] }).transactions ?? []))); }
