import { Badge, Table } from "@mantine/core"; import type { MonthlyOverview } from "../shared/api-contracts.js"; import { InlineBudgetAmount } from "./InlineBudgetAmount.js";
const money = (value: number) => new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(value / 100);
export function BudgetCategoryTable({ items, onSave }: { items: MonthlyOverview["items"]; onSave: (id: string, amount: number) => Promise<void> }) { return <Table.ScrollContainer minWidth={720}><Table striped highlightOnHover><Table.Thead><Table.Tr><Table.Th>Categoria</Table.Th><Table.Th>Planejado</Table.Th><Table.Th>Gasto</Table.Th><Table.Th>Disponível</Table.Th><Table.Th>Situação</Table.Th></Table.Tr></Table.Thead><Table.Tbody>{items.map((item) => <Table.Tr key={item.subcategoryId}><Table.Td>{item.categoryName} · {item.subcategoryName}</Table.Td><Table.Td><InlineBudgetAmount valueCents={item.plannedCents} onSave={(amount) => onSave(item.subcategoryId, amount)}/></Table.Td><Table.Td>{money(item.spentCents)}</Table.Td><Table.Td>{money(item.availableCents)}</Table.Td><Table.Td>{item.abovePlannedCents > 0 ? <Badge color="red">Acima do planejado · {money(item.abovePlannedCents)}</Badge> : <Badge color="teal">Dentro do planejado</Badge>}</Table.Td></Table.Tr>)}</Table.Tbody></Table></Table.ScrollContainer>; }
